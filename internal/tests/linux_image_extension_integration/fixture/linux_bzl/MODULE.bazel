module(
    name = "linux.bzl",
    version = "0.0.1",
)

bazel_dep(name = "llvm", version = "0.8.14")

linux_probe_tools = use_extension("//internal:linux_probe_tools_extension.bzl", "linux_probe_tools")
use_repo(linux_probe_tools, "linux_bzl_probe_llvm")
