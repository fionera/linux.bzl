/* This name is also absent from the ordinary prefix inventory. It becomes a
 * query demand only after the preceding round permits entry through header A. */
#if !defined(__linux_bzl_guard_round_b)
#define MAPPED_GUARD_ROUND_VALUE 0
#else
#error mapped guard round B must not be predefined
#endif
